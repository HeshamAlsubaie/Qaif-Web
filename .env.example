# QAIF frontend environment. Copy to .env.local and adjust.
# Base URL of the read-only QAIF FastAPI backend (see modules/api in the backend repo).
# No secret belongs in the frontend — this is a public origin only.
VITE_API_BASE_URL=http://localhost:8000
